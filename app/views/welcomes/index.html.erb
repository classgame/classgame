<img id="cover" src="https://pplware.sapo.pt/wp-content/uploads/2015/01/introprog22.png">
<div class="container">
<div class="row list_itens_center">
	<h1>Categorias de Cursos</h1>
	<p>Nossos cursos são dividos em categorias escolha uma e comece a aprender</p>
	<% @categories.each do |c| %>
		<div class="col-lg-4">
		<%= image_tag c.image, class:"image_radius" %>
		<h2><%= c.title %></h2>
		<p><%= c.description %></p>
		<p><%= link_to "Veja mais", c,class:"btn btn-default"%></p>	
		</div>
	<%end%>
</div>
<div class="row our_team" id="our_team">
	<h1>Nosso Time</h1>	
	<% @user.each do |u| %>
		<div class="col-lg-4">
		<%= image_tag u.avatar.url(:medium), id:"image-adm" %> <br>
		<p id="name_adm"><%=u.name%></p>
		</div>
	<% end %>
</div>
</div>
