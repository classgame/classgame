<%= simple_form_for @chapter, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :experience %>

  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to 'Cancelar', chapters_path, :class => 'btn btn-default' %>
<% end %>
